[project]
name = "python-app"
version = "0.1.0"
description = "Simple Flask API for demonstrating fullsend agent capabilities"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]
